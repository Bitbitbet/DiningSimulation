server:
  port: 23456 # 项目运行端口

spring:
  application:
    name: canteen-system  # 应用名称

  datasource:
    url: jdbc:mysql://localhost:3306/canteen_simulation?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver